<div class="col-12">
    <div class="row mx-0">
        <%- include('../components/label', { id, label: 'Birthday' }) %>
        <%- include('../components/input', { id, type: 'date', name, value, options: `max=${date}` }) %>
    </div>
</div>